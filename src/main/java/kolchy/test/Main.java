package kolchy.test;
import kolchy.entities.*;
import kolchy.service.*;
import java.util.Date;
public class Main {
   /* public static void main(String[] args) {
        ServiceEvent serviceEvent = new ServiceEvent();
        Date date=new Date(2024,1,15);
        // Example Event to add
        Event eventToAdd = new Event("Concert", "Auditorium", date, 50.0, "image.jpg");

        // Adding Event
        //serviceEvent.ajouter(eventToAdd);
        //System.out.println("Event added");

        // Modifying Event (assuming an ID of 1 for demonstration)
        /*eventToAdd.setId_Event(1);
        eventToAdd.setE_Name("Updated Concert");
        serviceEvent.modifier(eventToAdd);
        System.out.println("Event modified");*/

        // Displaying all events
        /*List<Event> events = serviceEvent.afficher();
        for (Event event : events) {
            System.out.println(event);
        }

        // Deleting Event (assuming an ID of 1 for demonstration)
        eventToAdd.setId_Event(1);
        serviceEvent.supprimer(eventToAdd);
        System.out.println("Event deleted");*/
    }
